# 本地功能集成到 main 的验收记录

## 范围

合并当前 TypeScript 工作台分支及其本地实现：邮件与求职任务、日历、受限每日更新、五角色外观和 Windows 桌宠。同意公开发布的仅是脱敏程序与公共素材，不包含个人数据库、密钥、授权缓存、简历或运行截图。

保留 main 已有的首页、海报和使用指南；不替换安装目录、重启正在使用的服务或迁移个人资料。公开源码更新不等于桌面安装快照自动升级。

## 本轮检查与修正

- 首轮 TypeScript 单元/接口/异常测试 252 项通过，Python 兼容与桌面回归 92 项通过；类型检查和生产构建通过。
- 生产及开发依赖审计使用官方 npm registry，0 已知漏洞；默认镜像未实现审计接口，不能把其 404 当作审计通过。
- 公开前新增忽略规则回归：截图、QA 产物、桌面配置、迁移回执及邮件设置原来未全部被忽略，先复现失败，再补规则后通过。
- 移除脚本对维护者绝对路径的依赖；文档路径改用用户目录占位，移除具体个人投递状态与数据库指纹。
- 桌面浏览器检查脚本不再假设某家公司的个人投递状态，限制为本机 GET/HEAD 请求，截图仍仅保存在临时目录。
- 岗位规则评测为固定合成样本：dev 27/27、reserve 13/13；不是模型真实准确率。

## 验收边界

- QQ、个人 Outlook、学校 Outlook 的真实登录与真实邮件识别准确率仍需各账号单独验收；此次不读取邮箱。
- 自动更新为运行期、显式授权、有额度限制的能力；退出后台后自动唤起、全网覆盖及自动入库不在已验收承诺中。
- Windows 桌宠尚未完成干净电脑安装包、多显示器/休眠恢复全矩阵及长时间资源测试。
- 模型连接测试成功仅证明该次配置可生成，不代表真实邮箱和每日采集整条流程已通过。

## 合并候选最终验收

2026-09-21 对候选提交 `e0350fe` 创建独立、干净的 detached worktree，从官方 npm registry 重新安装依赖。最终补充仅为本验收文档，不改变运行代码。

| 验收项 | 结果 |
| --- | --- |
| `npm ci --registry=https://registry.npmjs.org` | 87 个包安装成功，88 个包审计，0 已知漏洞 |
| `npm test` | 252/252 通过，0 失败、取消、跳过 |
| `npm run build` | TypeScript 检查、Vite 生产构建、服务端编译通过 |
| `python -X utf8 -m unittest -q` | 92/92 通过 |
| `git diff origin/main --check` | 无空白或补丁格式错误 |
| main 已有展示图片和海报 | 无修改，原样保留 |

### 浏览器交互验收

使用 `tests/fixtures/collection-ui-server.ts` 在独立临时资料目录和 `127.0.0.1:18771` 启动真实服务与浏览器。外部页面和模型均为合成 transport，不读取实际账户或调用真实付费模型。

- 公司搜索正常；修改测试公司的投递状态，等待写入后刷新，状态仍保留。
- 新建合成面试日程，填写北京时间 2026-09-25 14:00–15:00；列表正确显示，悉尼时间显示为 16:00，刷新后日程仍在。
- 来源检查完成 3/3：2 个来源读取成功，1 个明确标记 robots 拒绝；结果为「部分完成」，0 条候选、0 次模型调用，没有自动入库。
- 每日更新默认暂停，下一次检查尚未安排；展示不支持当前合成搜索接口及未确认匹配偏好的原因，未启用自动运行。
- 检查期间浏览器 error/warn 日志为空；页面非空白且主要模块正常呈现。
- 测试服务停止后，临时 `catalog.db`、`mail.db`、`qiuzhao.db`、`runtime.db`、`schedules.db` 的 `PRAGMA integrity_check` 均为 `ok`。运行期 runtime 锁定不作为数据库损坏或通过证据，以停止后的检查为准。

### 冲突与发布说明

仅 README 有文本冲突，已保留 main 的图文展示并合入当前功能、隐私边界和验证说明。没有业务代码冲突。测试结果是本机干净目录验收；仓库目前没有 GitHub Actions 工作流，因此不声称云端 CI 已通过。

个人正式资料库、密钥和已安装应用没有被更改。本次发布仍受上述真实邮箱、桌面安装矩阵及长期运行验收边界约束。
