# 阶段 1 / 2 复测记录

日期：2026-09-20。代码基线：`codex/typescript-migration` / `9666256`。

## 结论

**数据基础 T01–T05 通过本轮回归；模型设置子集通过工程和隔离浏览器验证。不能将原计划的前两个大阶段全部标成完成。**

尚未关闭的门槛：T06 原生悬浮助手动态目录桥接；T07 求职偏好/调度配置；T08 用户真实自定义模型服务的最小生成验收。本轮没有真实提供者凭据、没有付费调用，也不将模拟响应算作真实模型或岗位提取质量。

可以开始第三阶段的来源、契约、离线样本和只读抓取准备；完整手动采集验收仍须补齐对应前置条件。计划与标准见 [阶段 3 需求](阶段3-手动岗位采集需求.md)、[阶段 3 实施计划](阶段3-实施计划.md)。

## 实际执行结果

环境：Windows PowerShell；Node `v24.14.0`；在分享仓库执行，不在日常安装目录修改程序。

| 检查 | 本轮结果 | 证明范围 |
|---|---|---|
| `npm test` | 54 通过，0 失败，0 跳过；约 11.29 秒 | TypeScript 现有数据、接口、模型与客户端回归 |
| `npm run build` | 通过 | strict 类型检查、Vite 前端构建和服务端编译 |
| `python -X utf8 -m unittest -q` | 70 通过；约 6.71 秒 | 保留的原版行为回归 |
| 模型设置隔离浏览器流程 | 通过 | 填写→保存→连接测试→输入文本生成→刷新重新读取配置 |
| TS 预览资料只读核验 | 两个业务库完整性 `ok`，目录 315 家、进度 1 条 | 核验当时该资料的完整性及本轮进度未变，不代表其他资料或原版已全面复验 |

本轮未运行新的依赖审计；项目没有独立 lint 脚本，不额外声明 audit / lint 已通过。

### 数据基础覆盖

- 可信种子 315 家的字段、顺序与日期；重启幂等；别名/修订冲突；事务回滚和未来 schema 拒绝。
- 独立资料隔离、全部 7 种进度状态及原时间无损导入、WAL 一致性备份。
- 真实子进程中断后的锁释放、导入未半写与重试；这里不等价于物理断电或真实磁盘损坏测试。
- HTTP 本机来源/令牌/资料身份限制、非法请求拒绝，以及动态目录读取和客户端状态契约。

### 模型工程覆盖

- Windows DPAPI 真实加密/解密、保留/替换/清除密钥、原子保存、版本冲突、只读目标写失败。
- 本机 HTTP 合成服务的实际请求/响应路径；401、403、402、404、429、500、400、重定向、非法 JSON、空回复、限额、断连等异常。
- 请求取消、10 秒超时、并发调用/保存冲突、限流；敏感凭据不回显。
- 模拟响应只证明传输与应用处理，不能证明某家真实兼容模型服务的协议和输出质量。

## 浏览器验证详情

运行已有 `tests/fixtures/model-ui-server.ts`，使用独立临时目录和 `http://127.0.0.1:18769/`。fixture 注入本机 HTTP transport，页面中的 `fixture.example.com` 不会作为真实提供者访问；输入的 Key 是专用合成测试字符串，不是个人密钥。

实际观察：

1. 初始目录 315 家、0 条进度；没有写入用户资料。
2. 保存地址 `https://fixture.example.com/v1/chat/completions` 后规范化为 `/v1`，密钥输入框清空并显示“已加密保存 · 留空保留”。
3. 点击“测试连接”收到带“本机合成测试，不是真实模型”标记的回复。
4. 输入“阶段复测：请返回一句测试文本”，点击“发送并生成”，显示对应回复、`fixture-chat-only` 和 usage。
5. 回复内 `<script>这是文本，不应执行</script>` 以字面文本显示。
6. 刷新后重新进入模型设置，地址和模型 ID 保留，密钥仍不回显；状态恢复“已保存 · 尚未测试”，没有悄悄再次调用模型。

这是人工浏览器验证记录，不伪称新增自动化端到端测试。合成 fixture 不验证真实公网 DNS 或厂商鉴权，这些分别由安全负向测试和后续真实服务验收覆盖。

## 用户资料保护与限制

只读核验对象：`C:\Users\zz\AppData\Local\XiaozhaoGaogaoshou\profiles\typescript-preview`。

- `qiuzhao.db`：`PRAGMA integrity_check=ok`；进度 1 条；前后按公司名排序行内容的 SHA-256 一致：`a5a9c19c0a2d770a737b575b3b4baf1d4519a1c9f4274e8baf96d8c6f1475bb4`。
- `catalog.db`：`PRAGMA integrity_check=ok`；公司 315 家；schema v1。
- `runtime.db`：运行中的服务持有 `BEGIN EXCLUSIVE` 实例锁，另行只读检查返回 `database is locked`；本轮没有为了核验而终止用户服务，因此**未取得该文件的现场完整性结果**。锁机制的临时资料/崩溃恢复测试已通过，不能用其替代该现场结果。
- 原安装目录 `C:\Users\zz\AppData\Local\QiuzhaoLedger`、原桌面入口、每日自动化、真实内推码及投递记录均未修改。

本轮仓库只新增/更新需求、计划与复测文档；没有实现阶段 3、迁移业务 schema、安装依赖、切换服务或推送远端。
