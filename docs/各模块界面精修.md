# 各模块界面精修

## 范围与设计

延续五角色配色与原创绘画，只改 TypeScript 前端展示，不修改服务端、公司目录、数据库或投递状态。保留当前主线程中的邮件与自动更新改动，不合并或提交混合工作区。

- 模型设置：连接配置与试用区并列；统一字段间距，明确保存、测试、发送的不同作用。
- 面试日程：紧凑月历工具栏、纸张式月历、选中日期侧栏；新建表单分为基本信息、时间地点、准备复盘。
- 岗位更新：来源清单与匹配条件分栏，选择状态明确，核验和费用提示保留。
- 公司详情：档案式标题、清楚的属性行和操作区，长备注仍可完整阅读。
- 主题衣橱：展示现有五幅主题绘画，保留预览与应用的区别；不新增角色。

共同规范：标题 28–30px，正文 13–14px，辅助文字不小于 12px；低对比细边界、统一圆角、主题色主操作、角色只作为小面积点缀。无角色模式、浅深色模式、小窗口与键盘焦点均须保留。

## 验收

1. 所有原有字段 ID、data 属性与事件入口保留；不新增自动请求或模型调用。
2. 1440/1024/375/320px 宽度没有横向溢出；弹窗底部操作可达。
3. 浏览器实际打开各模块；日程切换/新建关闭、模型高级选项、更新视图、公司上下切换、换肤预览可用。
4. 浅深色与角色开关正常；五个主题绘画映射完整。
5. 前端构建、类型检查、针对性测试与全量测试分别记录，不把静态检查等同运行验证。

## 验证记录

- 前端构建通过；TypeScript `--noEmit` 通过。
- 全量测试：194/194 通过；包含模型接口、日程、持久化、采集安全等现有回归。后续 CSS 小修专项 3/3 通过。
- 新增模块展示测试：3 项，验证三种装饰图标差异、表单字段/事件属性唯一保留、角色关闭/小屏/减少动态效果及底栏约束。
- 真实浏览器：1440×900 下日历整月可见；日程编辑按三段展示，底部保存/取消可达；列表和月历、更新三视图、模型高级选项、详情下一家公司均可操作。
- 375/320px 下检查模型、日程、岗位更新、换肤与日程弹窗，未出现页面横向溢出；弹窗内滚动正常。
- 检查深色霁蓝与浅色琥珀；主题衣橱已改为现有主题绘画，关闭角色时插画/模块陪伴一起隐藏。
- 视觉回归中修正旧通用 footer 样式导致衣橱底栏只占半宽的问题；小窗口插画调整人物裁切，不做黑白滤镜。
- 1024px 模型、日程、更新、衣橱再次验证无横向溢出；衣橱头部关闭与底部应用操作常驻。五幅主题图均逐一切换检查。浏览器控制台 error/warn 为 0。已恢复正常窗口尺寸和此前的琥珀浅色外观。
- 实际预览为 `http://127.0.0.1:18765/` 的 TypeScript 版本；未改旧 Python 运行端口 18763，也未进行安装包发布。

## 边界

本次未发送模型请求、未联网采集、未新建实际日程、未变更投递状态或 API Key。页面读取和日程打开属于现有行为。空列表已验证；大量日程排布与真实模型回答长文本没有在用户资料中造数据演示。主线程新开发的每日更新/邮件业务未作为本次功能交付。
