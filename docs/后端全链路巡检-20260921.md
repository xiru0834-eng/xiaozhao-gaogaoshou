# 后端全链路巡检与修复（2026-09-21）

状态：本轮代码修复和隔离验收完成；真实旧后台切换、真实邮箱及收费模型联调仍未完成。范围为当前 TypeScript 应用及旧版回归，不等于穷尽所有输入和真实外部服务。

## 验收方法与数据边界

- 核对实际进程、端口、源码；保持个人资料、投递状态与已保存配置不变。
- 先运行既有测试，再按 HTTP／存储／模型／采集／调度／邮件／日程边界补充对抗性用例；发现缺陷先记录失败证据，再修复复测。
- 浏览器使用独立临时资料与无头 Edge，走正常控件和真实本机 HTTP、SQLite；外部邮箱、搜索与模型采用标记明确的协议替身，不产生真实费用。
- 检查认证、错误响应、幂等、版本冲突、并发取消、恢复、迁移、数据完整性，以及刷新／窄屏／跨模块操作。
- 本轮不提交或推送工作区其他未完成改动，不修改旧 QiuzhaoLedger 安装。

## 初始状态

- 分支：`codex/typescript-migration`，含大量前序未提交实现，本轮保留。
- 18765 正在运行旧 TypeScript 后台，未包含新任务能力声明；不会把源码测试通过误报为旧进程已经更新。
- 既有 Node 测试：222 项通过、0 失败。
- 隔离邮件任务浏览器：通过；1 次合成模型调用、3 个任务、0 个重复旧日程、0 页面异常。

## 10 个有失败证据的修复

每项均先观察失败断言／错误，再改实现，再运行同一用例；不是只凭源码推测。

| # | 复现的问题与影响 | 修复与验收 |
| --- | --- | --- |
| 1 | `/health` 固定写 catalog=2，实际库已是 3，且缺少邮件、日程库版本，容易误判升级状态。 | 四个 store 读取实际 PRAGMA user_version；新服务健康信息与四库逐项相等。 |
| 2 | 忽略一封邮件的所有事项，状态却显示已记录。 | 区分全部忽略、部分确认、待核对；全部忽略时正式任务为 0。 |
| 3 | A 窗口忽略整封邮件，B 窗口旧候选仍能确认成任务。 | 解密返回后、持久写入前重新核对忽略状态；旧窗口确认被拒绝，无任务新增。 |
| 4 | MailStore 未复用已有文件安全检查，可经硬链接修改另一个数据库。 | 打开前调用 safeFile；测试拒绝硬链接，原文件 schema、表和哨兵记录不变。 |
| 5 | 邮件加密等待期间，另一请求导入、忽略并清理同封邮件；迟到的导入会重新创建它。 | 加密后在同一事务内重查邮件与清理标记；返回原 ID，不复活已清理邮件。 |
| 6 | 缓存已有 1999 封时两个并发导入都成功，越过 2000 封上限。 | 事务内复核容量；仅一个成功，实际总数恰好 2000。 |
| 7 | 清理邮件写入失败未回滚，遗留事务锁，后续操作 database is locked。 | 异常时显式回滚；失败保留原邮件，解除故障后可重试清理，完整性 ok。 |
| 8 | 岗位采集尚在等待偏好／模型配置时关闭服务，关闭返回后采集仍可能启动。 | 关闭标志＋准备阶段等待；关闭后拒绝新启动，外部请求数、运行记录数均为 0。 |
| 9 | 每日更新等待 readiness 时关闭服务，迟到的检查仍可能启动搜索。 | 同步管理手动／定时准备阶段；等待结束后检查停止状态，不发出迟到的收费请求。 |
| 10 | HTTP 已接收的手工邮件事项正在解密时关闭后台，先关闭 mail.db 导致该请求返回 500。 | 先停止接收，关闭采集／模型工作，等待已接收 HTTP 请求处理完，再关闭邮件库及资料库。原请求返回 200，分析记录确实保存，重新只读打开完整性 ok。 |

专项用例在 `tests/backend-audit.test.ts`（8 项）、`tests/collection-service.test.ts`（关闭用例）、`tests/daily-service.test.ts`（关闭用例）。未改公司的投递状态、旧版布局、真实模型配置或邮箱账户。

## 回归与产品路径

最终当前工作区结果：

- `npm run build`：通过，包含 TypeScript 检查、Vite 与后端构建。
- `npm test`：235 项通过、0 失败、0 跳过；其中本轮新增 10 项专项，另有并行编辑的前端工作区测试，未将它们计为本轮修复。
- `npm run test:legacy`：78 项 Python 测试通过。
- `git diff --check`：通过。
- `npm audit --omit=dev --audit-level=high --registry=https://registry.npmjs.org`：0 已知生产依赖漏洞。默认 npmmirror 的 audit 接口返回 404，所以仅本次查询使用官方 registry，未改配置／升级依赖；这不是全代码安全证明。

以下均为无头 Microsoft Edge、真实本机 HTTP 和 SQLite；模型及网页上游使用合成协议替身，无收费调用。截图已人工目视核对桌面和 390px 窄屏，页面未横向溢出。

| 脚本 | 实际操作与断言 |
| --- | --- |
| `scripts/verify-backend-product.mjs`（本轮新增） | 修改公司进度并等待服务保存后刷新；配置模型而不调用；测试连接、生成文本、模型文本安全展示；刷新不回显密钥；认证失败不重试；只检查来源不调用模型；保存求职条件、提取原文；未确认前岗位库为 0，确认后为 1，刷新仍存在；投递状态不被岗位收录改变；390px 页面、0 页面异常。4 次合成模型请求、4 次合成网页请求。 |
| `scripts/verify-mail-tasks.mjs` | 粘贴合成邮件→模型提取多事项→编辑、部分确认→任务列表／日历；保存失败保留输入、重试、刷新、手工任务；完成／删除／恢复；五种皮肤的明暗模式、窄屏；旧后台能力提示。1 次合成模型调用、3 个任务、旧 events 无重复记录、0 页面异常。 |
| `scripts/verify-daily.mjs` | 保存时间／额度但不启动模型，手动检查、来信报告、多窗口只提醒一次、稍后看不反复弹出；启用／暂停、刷新、窄屏、跨模块导航；投递状态保留。1 次合成搜索、0 页面异常。 |
| `scripts/verify-schedules.mjs` | 新建日程、重开、编辑、准备清单、完成、列表、删除、日历、窄屏；0 页面异常。 |

全量测试还覆盖资料隔离／迁移与备份、HTTP token／profile／来源防护、模型错误／超时／取消、来源抓取安全、证据与字段校验、重复收录／修订冲突、时间精度／时区、邮件脱敏、事务／跨库恢复、每日额度／时钟边界与重复投递提醒。没有测量代码行覆盖率，不作“所有输入均通过”的结论。

复测浏览器时先构建，再执行 `node scripts/<脚本名>.mjs <playwright模块目录>`。本机模块目录为 `%USERPROFILE%/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright`。

证据文件：

- `artifacts/backend-audit/result.json`、`accepted-job.png`、`mobile-jobs.png`。
- `qa-companion/mail-tasks-result.json` 及 mail-tasks 截图。
- `artifacts/daily-verification/desktop-letter.png`、`mobile.png`、`mobile-dark.png`。
- 全量控制台日志：`%TEMP%/xiaozhao-audit-final.log`。

## 真实资料与运行版本

只读比较 `%USERPROFILE%/AppData/Local/XiaozhaoGaogaoshou/profiles/typescript-preview` 与 `backups/before-daily-2026-09-20T15-53-45-099Z-*.db`：四库所有原有表逐行一致，四个 `integrity_check` 均为 `ok`。该 TS 资料有 315 家公司、1 条投递状态记录、0 条原日程、0 个邮箱账户。该数量仅代表 TS 资料，不代表旧 QiuzhaoLedger 的投递总数；本轮未动旧安装。

收尾只读核验：18765 仍由 PID 52528 的旧后台提供服务，实例 `ba09d8da-f527-4262-b473-55a3831780ea`，健康响应仍为旧字段且无 recruitmentTasks 能力。构建成功不会替换正在运行的 Node 代码。此前自动停止旧后台被进程控制策略拒绝，本轮未换工具绕过，也未声称桌面已生效。

需要停止原 **TypeScript 18765 后台**后，在本仓库运行 `npm start`；不要关闭／替换 18763 的旧 QiuzhaoLedger，不要同时对同一资料开两个后台。仅刷新或关闭浏览器页不等于后台重启。重启后的个人资料迁移与实际入口验收仍待执行；隔离新服务启动／迁移／重开已测试。

## 尚未闭环／不能由本轮证明的部分

- QQ、个人 Outlook、学校 Outlook 的真实登录、租户权限、同步完整性及增量游标未做账号试点；现有资料没有连接邮箱。
- 未使用用户 API Key 调用收费模型，因此不能宣称真实供应商当日可用或邮件提取准确率；需要授权测试样本及冻结金标。
- 每日搜索仍是服务运行时定时执行；关闭后端／关机不会自动唤醒。新来源／内推码仍待证据核验，不等于自动验证有效或自动投递。
- 前序设计仍未完成的持久分析队列、人工草稿跨重启保存、邮箱增量、原生小窗任务联合摘要等，不因本轮通过而视为已实现。
- 人工故障注入覆盖事务失败、并发及正常退出，不等同于真实断电、长时间性能／稳定性测试或完整安全审计。

结论：本轮复现的 10 个问题已修复并通过对应回归，主要产品链路在隔离环境可行；不能承诺零 bug，也不能将隔离验收说成真实旧进程已经更新。
