# 五角色陪伴与机会清单精修

## 本轮边界

延续已有右侧改版；使用原有薄荷、霁蓝、绯樱、紫夜、琥珀五个角色，不重绘、不换画风。左侧导航结构、公司目录、个人投递状态、数据库与后端不改。正在并行开发的邮箱功能不调整。

## 设计和实施

1. 顶部加入当前角色的小型陪伴区：头像、名字、性格寄语和五角色快速选择。切换复用已有换肤偏好，保留明暗模式；关闭角色陪伴后全部隐藏。
2. 加入收起概览功能：隐藏顶部装饰、统计及提醒，留下状态栏、搜索与公司清单；随时可展开，不改变筛选或投递数据。
3. 精修公司卡片的字号、信息对齐、代码与操作区；保持主题色与业务状态色区分。
4. 主题衣橱中五个角色都展示头像，修正文案中的“四种”“只在侧栏”；保留仅预览和明确应用。

## 验收

- 五个角色的图片、性格、配色对应正确，切换后持久化；隐藏角色和深浅色可用。
- 卡片与紧凑列表、搜索、组合筛选、详情、复制继续可用；概览收起不丢失筛选。
- 320/768/1024/1440 像素窗口无横向溢出；1280×720 可以用专注模式增加首屏公司数量。
- 不伪造 Logo、职位可投状态或推荐码有效性；不往真实用户投递数据写测试记录。
- 类型检查、自动测试、生产构建，以及真实浏览器逐皮肤截图检查。

## 记录

2026-09-20，TypeScript 预览页 `http://127.0.0.1:18765/`：

- 类型检查通过；生产构建通过；当前全套自动测试 **173/173** 通过（包括本轮新增的五角色资源对应、概览折叠用例）。
- 真实浏览器逐一确认薄荷、霁蓝、绯樱、紫夜、琥珀的头像、选中态、寄语及主题对应；沿用现有资源，没有重新生成角色或引入远程图片。
- 琥珀深色、主题衣橱、原版头像尺寸、角色隐藏及重新显示通过；刷新后保留薄荷浅色与角色开启偏好。
- 搜索“滴滴”后折叠概览仍保留该搜索及 1 条结果；私企 + 待投递组合返回 213 条；清空后恢复 315 条。推荐码“核验说明”打开原有公司详情，保留公开线索未实测提示。
- 320×800、768×900、1024×768、1440×900 均无横向溢出；卡片分别为 1/2/2/4 列。320 像素下筛选菜单经过布局定位后底部为 788 像素，留在窗口内且内容可滚动。
- 1280×720 收起概览后，紧凑列表首屏完整显示 8 家；卡片模式完整显示 5 家。默认保留概览，用户可随时折叠。
- 最终恢复普通窗口、薄荷浅色、角色开启、卡片视图、概览展开及全部公司；本次浏览器记录无控制台 error。

边界与限制：本轮只改右侧展示与外观控件，未修改数据库、公司目录、投递状态，也未向真实投递记录写测试数据。18765 使用独立预览档案；页面的 0 条已投不能拿来判断旧版档案中的已投记录。旧双列截图来自哪个页面/缓存尚未确认，因此不声称已修复旧窗口自动更新。本轮未新增网络搜岗、有效内推验证或自动投递功能。复制按钮已点击检查，但未读取系统剪贴板，不将其记录为剪贴板内容端到端验证。
