# 机会清单：右侧视觉改版

## 范围与设计

依据用户提供的参考图，仅调整 TypeScript 工作台右侧，不照搬左侧导航；保留已选皮肤、深浅主题、公司目录、内推信息、投递状态及后端。当前实际运行入口为 `http://127.0.0.1:18765/`，不是旧 Python 台账。

采用「轻盈机会清单」：低饱和背景、轻量远山装饰、四块可读统计、胶囊式状态切换、近期截止提示和多列公司卡片。装饰不得遮挡公司列表；不引入虚构公司 Logo、收藏或匹配分数。公司名可点开完整详情，岗位关键词仅从已有原文提取，推荐码保留一键复制，完整出处与备注继续在详情中查看。

## 实施顺序

1. 定义并测试关键词摘要、截止提示的展示规则。
2. 重排右侧 HTML，复用现有筛选、复制、详情、状态持久化事件。
3. 替换仅作用于工作台的样式；默认卡片、可切换紧凑列表，自适应 1–4 列。
4. 类型检查、回归测试、构建；在真实浏览器核验深浅色、窄窗口和关键交互。

## 验收标准

- 左侧导航及旧版 HTML/CSS 不变；不修改数据库、公司数据或已有投递状态。
- 统计明确区分目录总数与当前筛选统计；截止提醒使用真实筛选结果，不声称已核实开放。
- 搜索、性质与行业叠加筛选、取消筛选、分组折叠、公司详情、内推复制、卡片/列表切换可用。
- 320、768、1024、1440 像素断点不产生内容横向溢出；弹层不被卡片裁切。
- 当前换肤仍生效，深色可读；交互有文字名称、键盘焦点与按下状态。
- 检查真实运行页面，不以静态源码或测试通过代替视觉验收。

## 数据边界

18765 是独立 TypeScript 预览资料目录，其状态不与旧版自动同步。此次仅改展示，不将旧版状态搬入，也不把预览中零已投误报为数据被清空。并行中的邮箱日程功能不在本次范围，保持其改动。

## 验收记录

本轮实际核验入口：`http://127.0.0.1:18765/`。

- `tsc --noEmit`、Vite 生产构建通过；最终全套自动测试 **171/171 通过**，0 失败。新增覆盖关键词来源约束、截止提醒文案和弹层边界计算。
- 对比仓库基线，左侧 `<aside>` 内容逐字一致；未修改旧版样式、后端或数据库。本次未通过真实用户资料写入投递状态来测试。
- 浏览器实测：搜索滴滴、推荐码复制成功提示、详情开关、私企＋AI 原生＋待投递组合筛选（25 家）、截止筛选（8 家）、清空筛选恢复 315 家、分组折叠、空结果恢复、卡片/列表切换均通过。
- 检查 1440×1000、1280×720、1024×768、768×900、320×900：分别适配多列或单列，无横向溢出。1280×720 下首排三张卡片及操作区可完整看到。
- 窄屏发现并修复筛选弹层超出底部：根据视口剩余空间翻转、限制高度并内部滚动；320×900 下展开底部为 888px，Escape 可关闭。
- 樱色皮肤的浅色、深色均作截图检查，恢复原浅色；浏览器控制台未见 warning/error。其他皮肤沿用既有变量，未逐套进行本轮视觉验收。
- 本轮验收不包括外部招聘官网可投递性，也不代表旧 Python 版与 TypeScript 预览资料已同步。
