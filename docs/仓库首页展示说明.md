# 仓库首页展示说明

2026-09-20。本次只更新 GitHub README、展示素材和使用文档，不修改应用界面、接口、资料目录、投递状态或启动器。

## 内容与排版

- 延续「薄荷石墨 · 轻量工作台」，用本地 SVG 封面、章节编号和真实截图建立产品识别；不依赖外部徽标服务，不宣称开源或持续集成已通过。
- 首页先讲产品和界面，再说明操作流程、快速上手、能力边界及开发入口。原 README 的完整操作说明保留在 `使用指南.md`，相对链接随迁移调整。
- 功能描述以 `50a5daf` 为基线：采集仍是基础模块，未宣称自动搜岗或自动投递；区分 Python 悬浮助手与 TypeScript 独立资料，区分进度标记与真实投递回执。

## 截图来源与隐私

- 工作台、紧凑清单、深色主题和模型设置截图：从此提交的真实构建页面生成，使用新建的隔离临时资料和随机本机端口；全部个人状态为空，模型未配置，没有真实密钥或外部模型请求。
- 悬浮助手截图：真实 `companion.html` 搭配公共目录及内存空状态桥接。只展示 UI，不作为原生窗口置顶、贴边或真实保存验收。
- 不使用用户日常窗口、登录浏览器、个人数据库或用户上传的投递截图。公司及推荐码来自仓库已有公共快照，图片不是时效性认证。
- 图片统一放在 `docs/images/`，SVG 封面只包含文字和几何图形。刷新素材时应继续采用隔离资料，不截取个人进度或已填写密钥。

## 验收范围

检查相对链接和图片文件存在、README 标题锚点可达、桌面/窄窗口文档排版、真实截图内容、文档差异及 Git 暂存范围。此次不改应用功能，不将文档渲染检查冒充产品全链路测试；产品测试数字明确绑定此前验证过的提交。

## 本次检查结果

- 5 张真实页面 / 视图预览截图均已人工查看，生成时浏览器 `pageerror` 为 0；模型界面未配置密钥，小助手与新版投递进度均为空。
- 使用 GitHub Markdown API 获取实际 Markdown 渲染结果，再在隔离 Edge 中配合近似 GitHub 样式检查。1280px 与 390px 宽度无整页横向溢出；这不是已登录 GitHub 仓库页面的像素级验收。
- README 和两份文档的 29 处相对资源引用存在；4 个导航锚点均可解析；7 个图片元素加载成功（包含封面、5 张截图及已有吉祥物）。
- `python -X utf8 -m unittest -v test_share`：4 项分享 / 隐私边界测试通过。`git diff --check` 通过。应用源码与运行数据未修改，没有新增项目依赖。
- 首页所列 127 项 TS / 70 项 Python 和构建通过，引用的是同一对话前一轮对 `50a5daf` 的实测结果，本次仅文档修改不伪称重新进行了完整产品验收。

## 2026-09-20 · 品牌宣传海报追加

- 用户明确要求以宣传为主，不放过多真实内容。因此本轮生成仅以现有吉祥物为参考，不提供真实截图、简历、公司列表、投递进度或密钥。
- 使用内置图片生成工具完成三张 1536 × 1024 海报，保留原始 PNG 于 `docs/posters/`：机会工作台、贴边小助手、本地优先。分别采用暖白、石墨夜景与柔和薄荷场景。
- README 首屏替换为机会主题海报，并新增品牌海报导航和两张并排预览；已有真实 UI 截图及功能限制说明保留。每张都能打开高清原图。
- `docs/posters/README.md` 保存文案、能力边界与复用说明；`prompts.md` 保存实际生成提示词和验收约束。品牌插画与真实截图明确区分。
- 海报 02 描述兼容版助手，不宣称已经连接新版资料；海报 03 注明模型调用会发送输入、可能计费，本地文件插画不构成整库加密承诺。
- 只新增宣传素材和文档，不改源代码、数据库、投递状态、桌面入口或主线程正在开发的内容。
- 验收结果：三张海报逐张目视检查，主标题、功能短句与必要说明可读，无真实求职数据；PNG 尺寸均为 1536 × 1024，合计约 5.2 MiB。
- GitHub Markdown API 渲染后，在隔离 Edge 中检查 1280px / 390px：首页与海报区无整页横向溢出；45 处相对链接存在、9 个图片元素加载、5 个导航锚点可达，页面错误为 0。样式是本地近似，不声称 GitHub 实际站点像素级一致。
- 分享 / 隐私回归 `python -X utf8 -m unittest -v test_share`：4 项通过；`git diff --check` 通过。没有修改应用功能，因此没有将这一轮文档验证写成重新完成全套产品验收。
