# 工作台：薄荷石墨 · 紧凑精修

## 范围与设计

2026-09-20，针对完整工作台的筛选与公司列表精修。沿用原角色、薄荷品牌色和石墨导航；不更改后端、公司目录、公司顺序、投递状态或个人备忘录。

- 筛选区：搜索和筛选为第一层，筛选标签、结果数、视图设置为第二层。缩短空白，保留组合筛选和清空入口。
- 公司列表：公司名称与性质并列，岗位另起一行；缩小图标及行间距。长名称仍可折行，岗位保留悬浮全文和详情入口。
- 主次：公司名称最醒目；推荐码保留明确复制按钮；城市与截止日期为辅助信息。截止提醒只强调必要信息，不再将整块区域涂成警告色。
- 操作：状态和官网入口并列、对齐。保留分组收起、紧凑视图、主题、详情抽屉和键盘焦点。
- 小窗口：筛选区自然换行，列表按宽度重新编排；不通过缩小整页或藏掉关键操作解决空间问题。

## 验收目标（修改前制定）

1. 在 1440×900、100% 视口、默认密度及所有公司条件下，首条公司顶部不高于 340px；普通双行公司行不高于 82px；首屏完整公司不少于 6 条。
2. 实测 320、768、1024、1440 像素宽度，无横向溢出；弹出筛选和详情不被容器裁切。
3. 测试搜索、组合筛选、清空、分组收起、密度切换、主题、复制与详情；不在真实数据上改变投递状态。
4. TypeScript 与旧版工作台的 CSS 完全一致；构建、TypeScript 测试、旧版回归通过。
5. 安装前备份 index.html；仅替换样式段，正文与脚本逐字保持；真实数据库完整性通过，状态记录不减少。

## 修改前测量

真实安装版 18763，1440×900：筛选区 155.33px，首条公司顶部 440.38px，首行 98.46px，首屏完整显示 4 条公司。

## 验收结果

### 浏览器与布局

实际安装版 `http://127.0.0.1:18763/` 已刷新验收，仍显示 315 家公司、103 家已投递。

| 1440×900 默认首屏 | 修改前 | 修改后 |
| --- | ---: | ---: |
| 筛选区高度 | 155.33px | 98px |
| 首条公司距顶部 | 440.38px | 339.79px |
| 首条公司行高 | 98.46px | 78px |
| 完整可见公司 | 4 | 7 |

紧凑视图首行 62px，同条件完整显示 8 家。数字来自浏览器 DOM 几何测量，截图同时检查；实际数量随窗口、缩放、长名称和筛选结果变化。

320×780、768×900、1024×768、1440×900 无页面横向溢出。检查了明暗主题；320 和 768 宽度的行业筛选弹层在视口内。紧凑布局并未缩放整页，默认公司名仍为 15px、岗位 13px（手机相应为 14px、12px）。

### 交互回归

在 `preview_isolated.py` 创建的临时数据库预览 18769 上完成，未操作真实投递状态：

- 私企 + 待投递 + Agent 搜索组合得到 73 家，全部符合私企条件；清空后恢复 315 家。
- 截止分组收起后公司行隐藏，展开后恢复；密度切换保留列表。
- 公司详情打开正常，Escape 关闭后焦点返回原公司名称。
- 复制滴滴公开推荐码，按钮显示已复制，剪贴板内容确为该码。
- 无匹配搜索显示空结果和重置入口，清空恢复完整清单。
- 临时库中更改滴滴为笔试，出现已写入提示；重载后仍为笔试。
- 浏览器捕获的 error / warn 为 0。

### 自动检查与保护范围

- `npm test`：39/39 通过（新增两个样式一致性与控件保留检查；不将它们当作视觉测试）。
- `npm run build`：类型检查、Vite 构建和服务端编译通过。
- `python -X utf8 -m unittest -q`：70/70 通过。
- `git diff --check`：通过。
- TypeScript 样式、仓库旧版内嵌样式、安装版内嵌样式逐字一致。
- 安装版替换前后移除 `<style>` 段的全文完全一致：没有改写 HTML 正文、脚本、DATA 或 APPEND_DATES。
- 真实 SQLite：更新前后均为 112 条保存状态，`PRAGMA integrity_check=ok`；数据库 SHA-256 未变化。
- app.py、companion.html、floating_model.py、floating_window.py、companion_activity.py 的 SHA-256 均未变化。

### 安装、恢复与边界

只替换安装目录 index.html 的 CSS 段，无需重启后台服务。旧版文件备份：

`%USERPROFILE%\AppData\Local\QiuzhaoLedger\backups\index-before-workbench-polish-20260920.html`

如用户窗口还缓存旧页，在没有未保存操作时刷新即可。18763 是现有真实台账；18765 为另行启动的 TypeScript 预览，不应混淆两套数据目录。本次不启动或迁移其个人数据。没有推送、合并或修改后台接口。
