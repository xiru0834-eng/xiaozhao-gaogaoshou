# 实施计划：每个人电脑上的 AI 求职更新

> 当前状态：阶段 1 数据基础 T01–T05 已完成；模型设置子集和阶段 3 手动采集工程版已实现。用户真实接口、三家真实岗位提取质量、T06 两端同步仍待验收。见 [实现与测试记录](../docs/阶段3-实现与测试记录.md)。下方保留原分阶段路线及历史结论。

日期：2026-09-20。范围：本机独立运行、自己的模型/搜索配置、自动增量更新。状态：TS 工作台、数据基础和模型设置子集已实现；采集、搜索、调度仍未实现。没有修改真实数据库或定时任务。

规格：[本地 AI 更新功能规格](../docs/本地AI更新功能规格.md)；执行清单：[todo.md](todo.md)。

## 一句话顺序

**保护数据与副本隔离 → 模型设置 → 一条来源的手动完整流程 → 扩展来源和内推 → 每日自动运行 → 干净电脑分享验收。**

不要先写定时器。可靠性问题必须在可观察、可手动重试的流程里解决，再放到后台运行。

## 第一批具体先做哪里

已完成前置 P0：拆分 TypeScript 模块、严格类型检查、315 家目录一致性、独立预览服务和 SQLite 保存/备份。证据见 [TS 迁移记录](../docs/TypeScript迁移.md)。这不等于 T01–T03 全部完成。

接下来先补齐 T01–T03：全部旧状态的契约、资料实例身份、显式无损导入。然后完成 T04–T06 动态目录与桌面外壳。现有视觉和业务状态保持不变，不需要密钥、不调用付费服务，也不接管现有桌面入口。新增业务一律 TypeScript；Python 仅保留兼容回退。

交付证据：两个测试配置相互隔离；不会误连端口上另一份台账；临时旧库迁移前后的公司名、状态、更新时间逐条一致；失败时仍可打开旧副本；现有 70 项测试与新增测试同时通过。

下一批 T04–T06 让完整版和悬浮助手从同一目录服务读数据，旧 315 家公司名称/顺序保持一致，再允许本地新增。模型设置放在这之后，避免先接通 API 却没有安全的数据落点。

## 实施依赖

```text
T01 基线 → T02 独立运行 → T03 导入与恢复
                         ↓
                    T04 目录存储 → T05 完整版目录 → T06 悬浮窗目录
                                                     ↓
                   T07 设置/凭据 → T08 模型连接与脱敏
                                                     ↓
                   T09 安全读取 → T10 提取与固定评测
                                                     ↓
                   T11 预览/入库 → T12 真实来源手动验收
                                                     ↓
                   T13 搜索发现 → T14 内推与来源扩充
                                                     ↓
                   T15 单次worker → T16 定时/补跑 → T17 自动入库/通知
                                                     ↓
                   T18 分享安装与升级验收
```

按上下游顺序实现。独立样本标注、适配器测试和文档可并行，但同一数据库迁移、目录接口和入库规则不并行改；未获用户要求不自动开启多 agent。

## 阶段 A：原应用不退步，数据不串库（T01–T06）

目标：为新增能力提供安全落点，保持已认可 UI。工作在分享仓库，不在日常安装目录直接改动。

- 初始测试使用临时目录和空闲端口。当前 `launcher.py` 只验证应用名，新增实例/配置身份检查后，不复用不同配置的服务，也不强杀占用进程。
- `qiuzhao.db` 的现有业务表和 API 继续兼容；旧数据导入是显式动作，不扫描整个用户目录自动寻找数据库。
- 建立 `catalog.db`，通过可信种子初始化目录。种子不代表岗位当前可投；不重新解释旧投递事实。
- 完整版、悬浮窗切到同一目录读取服务。页面老数组作为过渡种子保留，停止让新采集逻辑写 HTML。新公司顺序由数据库追加序号保证。
- 当前 `test_backend_file_is_unchanged` 是旧 UI-only 任务的文件摘要保护。真正实现后端变更时，须先增加旧接口、状态枚举、事务和拒绝非法请求的契约测试，经审阅再替换该摘要检查，不能直接删测试。

检查点 A：两端目录与状态一致；新旧数据兼容；使用前后无顺序、筛选、导出、保存重试回归。只有临时配置验证完成，仍不切换用户日常安装。

## 阶段 B：模型设置能用、能失败、能保护密钥（T07–T08）

目标：设置页可填写脱敏求职偏好、模型连接信息并测试，重开仍生效。密钥存 Windows 当前用户凭据存储，不在源码、设置 JSON、日志或浏览器 localStorage。

先支持一个明确的云模型协议，允许用户配置地址和模型；接入前固定支持范围。错误密钥、模型不存在、超时、限流、余额不足和不兼容输出分别显示可操作的原因。不自动买额度，不假设网页聊天会员就是 API 额度。

无模型配置时，原台账功能仍可用；“连接成功”只证明连接，不代表岗位判断准确。Ollama 在同样接口和测试契约下后续接入，不阻挡第一条云模型路径。

检查点 B：真实密钥只由用户在本机设置页输入；模拟错误路径必须通过，允许后才做最小真实连接调用。

## 阶段 C：先跑通一条真实来源的手动完整流程（T09–T12）

目标：点击检查 → 官方页面 → 结构化候选 → 原文证据 → 去重/规则 → 差异预览 → 用户接纳 → 两端看见新增。

先做无需登录的公开来源。稳定公开 HTML 或明确可用接口优先，动态网页/验证码先记为受限，不先搭任意浏览器操控 Agent。提取模块和联网模块可用离线样本独立测，但不能用离线通过代替真实验收。

批次状态：排队、运行、部分完成、完成、失败、取消/需配置。预览与入库分离，入库原子且幂等；重复点击不重复新增。模型只提交候选，不能直接写库或调用系统命令。

公司级旧进度不继承到新岗位，也不因此屏蔽同公司新岗位。第一版保留公司级记录，不把岗位级投递管理捆绑进来。岗位首次发现与岗位发布日期分开；页面未提及的要求就是未知。变化记录保存证据与前后值，失败不清空旧目录。

检查点 C：至少 3 家真实、可访问官方来源手动跑通；同一批次重复接纳不增行；中断后可恢复；更新时个人状态仍可正常保存。此时才可称“接模型后的岗位更新流程可用”。

## 阶段 D：从关注清单扩到发现新公司（T13–T14）

目标：不是仅复查 315 家，而是通过关键词和时间范围发现清单外机会；同时维护公开内推线索。

先增加一个搜索提供者适配器。默认路线是 Tavily，但实际可用性、预算和中文招聘覆盖须在 T13 前核对；可替换供应商，不改变后续候选/证据/去重流程。无搜索配置时清晰标明仅定向检查。

收集分两条队列：已有官方来源复核 + 新来源探索。24 小时不足再扩 7/30 天，记录真实覆盖，不声称穷尽全网。相似职位不能仅凭公司/标题合并，多城市与岗位 ID 按官方事实处理。

内推记录严格按公司/批次绑定。来源声称有效仍为未实测；无法公开获取的说明需本人联系，不自动私聊。

检查点 D：扩到约 20 家、至少 3 类来源；报告访问成功/受限/失败比例、误收与漏收样本、推荐精确率，不以收录数量替代质量。

## 阶段 E：最后加每天自动运行（T15–T17）

目标：手动和计划任务调用同一 worker；开关、预算、时区、补跑、失败可见。

- 用户显式开启后才注册 Windows 当前用户任务。程序界面关闭不影响 worker；用户注销、关机时不承诺运行。
- 时区独立保存，默认建议由用户确认；悉尼与北京时间不同，夏令时切换须测。后台短周期检查到期状态，说明可有调度延迟。
- 同一配置加锁，持久化当天运行记录；重启补跑一次，不能重复补过去多天。异常退出用租约恢复，不永久卡在运行中。
- 默认自动采集到待核验区；通过验收后可启用“可信来源合格新增自动追加”。未知毕业窗口、可疑来源、官方关闭不明确的记录不能偷偷升级为可信推荐。
- 同时限制运行时长、每源请求量、模型调用量与日额度；重试计入预算。额度未配置时使用保守上限，真实付费前明确展示。
- 完全失败与真正零新增分开。应用内显示最近成功时间、失败原因、下一次检查时间以及重试入口。

检查点 E：冻结时钟测试覆盖夏令时、跨日、离线恢复、并发触发、异常退出和额度耗尽；短间隔真实计划任务验证成功后，再观察至少两次计划触发并确认无重复。不能用“已注册任务”冒充“已稳定运行”。

## 阶段 F：别人下载后可独立使用（T18）

目标：干净 Windows 用户/机器完成安装、配置、采集、重开与程序升级。每人数据独立，无维护者凭据，不依赖 Codex 特殊路径或打包前的 vendor 文件。

代码更新和岗位更新分离。先保留人工 Git 更新和有文档的重启流程，不做自动强制 `git pull`、覆盖工作区或执行未审核更新。程序版本和数据库 schema 版本不兼容时明确拒绝启动并提供恢复指引。

最后才决定是否切换维护者的真实日常应用；切换前先备份并核验，旧 Codex 每日任务要显式调整为不重复写同一套资料，但本轮不修改它。

检查点 F：新增用户从空白进度开始；原用户更新后状态完整；API Key 和数据库不在 Git；卸载后台计划任务后不再运行；已知权限/依赖问题记录到安装文档。

## 评测与质量门槛

拟建立 60 条标注用例：20 条明确匹配、10 条硬性不符、10 条条件未知/受限、8 条重复/多城市、6 条关闭/异常、6 条恶意内容/异常结构。40 条开发、20 条冻结验收，分层划分；用例文件记录原始来源或“合成”，数据版本和预期结果。不是已完成的数据集。

每次阶段验收报告：现有回归、新增单测、固定集结果、真实运行范围、失败样本、来源覆盖、实际消耗和未闭环风险。

硬门槛先于模型平均分：进度零误改、重复零新增、网络错误零误下架、所有推荐可追溯、密钥不泄露。推荐精确率目标至少 95%，同时单独报告召回/不确定比例；20 家试点之外不宣称总体覆盖率。

## 风险与控制

| 风险 | 控制 |
|---|---|
| 副本误连原服务 | 配置身份与端口检查；开发仅临时目录 |
| 脱离 HTML 后两端目录不一致 | 同一目录服务，先兼容迁移再撤旧路径 |
| 模型/搜索成本失控 | 有界运行、预算预留、重试计费、缓存和可取消 |
| 页面改版/访问受限 | 每源状态、适配器样本、降级待核验，不误下架 |
| 网页诱导执行/内网访问 | 内容视为数据；无任意工具执行；抓取地址/重定向校验 |
| 迁移丢记录或更新覆盖 | 一致性备份、逐条比对、失败不切换、个人库与采集分离 |
| 计划任务重复或漏跑 | 锁、日键、租约、补跑、持久化运行记录 |
| 旧自动化与新采集器双写 | 正式切换时单独确认任务职责；新采集不写 HTML |

## 本轮检查记录

先完成原代码检查与规划，后按用户选择完成 TS 工作台和状态服务基础迁移。新版 12 项测试、旧版 70 项回归通过；浏览器核验见迁移文档。AI 更新 T01–T18 仍未整体交付，未接入模型、未付费、未注册 Windows 任务、未修改旧自动化或真实个人库，也未推送远端。

附带技能的 `references/definition-of-done.md` 在本机未提供；验收以本计划明确列出的回归、运行证据与质量门槛为准。

## 2026-09-20：模型设置先行子集

用户确认使用自定义 OpenAI 兼容接口，本轮独立实现 T07/T08 的模型子集：保存 → 短文本测试 → 用户输入实际生成。功能采用现有 TS 后端、Windows DPAPI、独立原子配置文件和受保护本机 API；不等待 T06，也不因此把助手桥接、求职偏好、自动搜岗或每日任务标成完成。

已通过 53 项 TS、70 项旧版回归、严格构建和官方 npm audit；浏览器隔离流程、真实 Windows 加密及只读目标写失败验证完成。**用户真实服务生成仍未验收**，由用户在模型设置页输入凭据后完成。功能/非功能需求、具体标准和证据统一放在 [模型设置需求与验收](../docs/模型设置-需求与验收.md)，不把连通性测试当成岗位提取质量验证。

## 2026-09-20：前两阶段复测与第三阶段详细规划

代码基线 `9666256`：本轮 54 项 TS 测试、70 项 Python 回归、严格构建通过；本机合成模型的保存/连接/生成/刷新已在浏览器验证。TS 预览资料的进度行摘要前后一致，公司目录 315 家，两个业务库完整性 ok。详见 [复测记录](../docs/阶段1和2-复测记录-20260920.md)。不能据此宣布 T06、完整 T07 或真实模型验收通过。

第三阶段指 C / T09–T12，细拆 C0–C9：先建立来源/样本/契约与只读抓取，再做有证据提取、候选预览、原子接纳，最后真实试点。需求包括功能、非功能限制、API、迁移策略及 60 例固定评测设计；样本尚未建立、代码尚未实现。文档见 [阶段 3 需求](../docs/阶段3-手动岗位采集需求.md) 和 [阶段 3 实施计划](../docs/阶段3-实施计划.md)。

C0/C1 可不依赖真实 Key 先准备；网页闭环与两端闭环分开验收。每日自动运行、全网搜索与自动投递不提前混入本阶段。本轮不修改个人状态、原安装、定时任务或远端仓库。

## 2026-09-20：用户确认后的阶段 3 实现

C1–C8 的网页版工程路径已实现：登记来源检查、求职条件、有证据的有限提取、持久化候选、差异预览和原子接纳。具体文件按职责拆分，测试合并为 collection/source/eval 套件；不拘泥于规划占位文件名。

最终 140 项 TS、70 项 Python 回归及严格构建通过；固定 60 例仅为合成规则回归。浏览器假模型完整路径和三个真实入口的只读检查通过，其中 Kimi 明确受限、MiniMax 仅入口。尚无三家真实岗位提取或真实模型质量结果；C0 独立金标、C9 真实试点和 T06/T12 两端验收保留未完成。

新功能只在独立 `stage3-preview-20260920` 资料、18770 端口展示；没有迁移真实个人状态、修改原桌面启动器或推送远端。复现和下一步见 [阶段3-评测报告](../docs/阶段3-评测报告.md)。

## 2026-09-20：API 驱动自动搜岗与内推设计（提案，未启用）

根据用户希望每人使用自身 API、定时搜索更新的要求，补充 [框架与 API 方案](../docs/自动岗位与内推更新-架构与API方案.md) 和 [ADR-002](../docs/decisions/002-local-discovery-scheduler.md)。它细化 T13–T18，不覆盖既有验收记录。

核心选择：SearchProvider 与 ModelService 分开；单一 LocalEngine 持有资料，系统 dispatcher 仅触发到期检查；自动采集和自动接纳分离；新增内推适用范围/出处模型、持久化预算、IANA 日键及受控补跑。先做 D1/D2 搜索配置与一次可见手动搜索，证明真实中文来源可获取，再逐片实现内推、预算和调度。真实 C9 门槛不跳过。

本次只读核对当前代码、查阅官方 API/Windows 文档并写设计，没有运行收费请求、改变真实资料或启用任务。服务商和实际预算、定时开关、自动接纳授权仍待确认。

## 2026-09-20：邮箱识别笔试／面试日程（新计划，待审阅）

需求、功能／非功能边界、数据／接口、M00–M19 任务及评测门槛见 [邮箱日程助手-需求与实施计划](../docs/邮箱日程助手-需求与实施计划.md)，关键决策见 [ADR-003](../docs/decisions/003-mail-schedule-review.md)。这条计划与自动搜岗并列，不替换或勾选上方任务。

默认建议：全部结果先人工确认入历；QQ IMAP 和 Outlook Graph 分适配器；邮箱授权及发送私人内容给模型分别同意；近 30 天指定文件夹，后台默认关闭。先 M00/M01/M03/M04，再形成 M08 的手动通知闭环；随后 QQ、Outlook、定时同步、真实发布验收。Outlook 应用注册条件在开工前期核对，避免晚期受阻。

检查点 A 粘贴识别与确认 → B QQ 真实只读闭环 → C Outlook 真实闭环 → D 两次真实后台同步 → E 数据／隐私／干净用户发布验收。每阶段有独立测试证据，工程模拟不能替代真实授权联调。

本轮仅新增文档和索引，未读真实邮件、调用模型、修改代码／数据库、安装依赖、创建定时任务或推送远端；任务全部待实现。计划批准后仍由用户在本机完成邮箱与模型传输授权。

## 2026-09-21：每日更新与一次性角色来信（深入设计）

用户已确认在软件内独立运行、可设时间、沿用当前二次元角色、每天一次轻量提醒，并要求先产出深入设计。新增[详细设计与验收规范](../docs/每日更新与角色来信-详细设计.md)和[ADR-004](../docs/decisions/004-daily-update-and-companion-inbox.md)。本段只追加，不改写旧设计和完成记录。

已核对当前源码：只有邮件定时器，岗位搜索、每日调度与持久来信尚未接入。前序临时诊断已用DeepSeek官方搜索取得HTTP 200、1次真实搜索及10条来源；这只关闭连通性疑问，不关闭中文覆盖/真实岗位提取/内推有效性的验收。搜索提供者路线先实现DeepSeek原生适配，现有OpenAI兼容模型和邮件通道保持。

设计把功能拆为N01–N15，先完成契约、时钟、存储和预算，再做搜索/原文/内推、单轮日报和UI，随后后台timer、单一engine/Windows唤起，最后真实质量与连续运行验收。R1手动、R2后台运行期、R3系统唤起、R4质量与可选自动收录分别标识，不用阶段成果代替整体完成。

一次性提醒采用后端唯一日回执和事务领取；同日多窗口/刷新/重启不重复提供，崩溃漏看由未读历史兜底。自动发现和自动收录分开；新公司末尾追加，不改个人状态。默认建议07:00悉尼，用户在界面确认实际时间、额度及启用；不从聊天密钥静默设置永久收费任务。

本轮只编写设计/ADR/任务清单及文档检查，未实现新接口、未运行收费请求、未改数据库/运行进程/系统任务，也未推送Git。完成门槛及命令详见设计第12–14节。

## 2026-09-21 每日更新实施记录

本轮按「每日更新与角色来信-详细设计」落地 N01–N04、N07/N08 基础、N09/N10 基础与 N11，先补失败测试再逐片实现。已在隔离资料完成搜索协议替身、实际 HTTP/SQLite、真实无头 Edge 的正常/边界/异常路径；备份后接线现有 18765，四库原有逻辑内容逐项不变。

范围差距保留：N05/N06 来源/内推完整核验、N12/N13 Windows 唤起、N14 自动接纳、N15 真实冻结集和两轮真实日调度未完成。当前资料无模型凭据与求职偏好，未替用户启用收费操作。不得据此声称“完整每日岗位与内推更新已完成”。

详细证据：[实现与验收](../docs/每日更新与角色来信-实现与验收.md)。后续先扩展有证据的来源和内推数据管线，再完成安全后台唤起；用真实质量门槛决定自动接纳是否开放。

## 2026-09-21：邮件转笔试／面试／测评任务（设计提案）

用户要求先分析如何实现并产出设计，不授权本轮直接实施。已核对邮箱第一版与日程源码，并在隔离测试中复测 16 项专项测试全部通过；该结果只覆盖现有基础能力。

新增[邮件转求职任务详细设计](../docs/邮件转求职任务-需求与详细设计.md)及[ADR-005](../docs/decisions/005-mail-actions-as-recruitment-tasks.md)。采用一封多事项、候选人工确认、正式任务唯一主记录、日历联合只读投影；严格区分固定场次、截止、窗口与时间待定。保留旧 events 和投递记录，不把准备清单误当正式任务，也不双写日历副本。

推荐顺序：MT01–MT04 手工任务安全可用 → MT05–MT08 邮件多事项及确认恢复 → MT09 日历联合视图，形成 R1；随后 MT10 变更、MT11 队列、MT12a/b 双邮箱增量、MT13 小窗形成 R2；MT14a/b 冻结评测与真实联调形成 R3。每片范围、依赖、验收见详细设计第 12 节。

本轮仅文档，未改代码、数据库、进程、邮箱或模型配置。真实邮箱、模型发送内容、付费额度与后台启用继续由用户在本机明确选择。既有 M/N 任务和未关闭的质量门槛不因此勾选完成。

## 2026-09-21：邮件转求职任务实施增量

用户随后确认按文档实现。本轮完成 R1 核心链路：严格任务／时间契约、兼容迁移、任务 API 与编辑器、邮件多事项提取与证据核对、批次确认／跨库恢复、日历联合投影；MT10 实现显式目标选择后改期／取消／来源关联。旧 events 和公司投递状态保持，不复制日历副本。

新增 16 项专项测试，全套 222 项 Node、78 项旧版 Python、类型检查、构建及隔离 Edge 浏览器流程通过。真实邮箱与收费模型未调用，不能把协议替身测试视为真实提取质量。证据和实际接口取舍见 [实现与验收](../docs/邮件转求职任务-实现与验收.md)。

18765 后台切换被进程控制策略拒绝，原服务仍运行；已补旧后台能力检测，保留原日程并提示重启。实际四库原表与备份逐行一致、完整性 ok，个人资料尚未执行 v2 迁移。需用户停止旧 TypeScript 后台后运行 npm start 才能启用新版。

尚未完成：未确认编辑草稿跨重启持久化、持久分析队列与邮箱增量、小窗联合摘要、完整变更差异面板、冻结金标及 QQ／两类 Outlook 真实试点。下一片优先草稿与小窗，再做增量／真实评测；不把上述核心实现标为 R1–R3 全部发布通过。

## 2026-09-21：后端巡检与现场修复

按用户要求，先核对运行资料／既有工作区，再运行全量基线、跨模块边界测试和隔离浏览器，失败用例先于修复。10 个缺陷完成红→绿；235 项 Node、78 项 Python、构建和四条产品主流程通过。个人资料只读逐行比对，原有记录不变。完整复现及覆盖边界见 [后端全链路巡检](../docs/后端全链路巡检-20260921.md)。

代码与构建已更新，18765 的旧进程没有切换；不绕过先前进程控制策略。接下来仍需用户停止旧 TS 后台后 `npm start`，才能做个人资料新版入口验收；真实邮箱、收费模型准确性、跨重启草稿与增量队列按原计划独立验收。

## 2026-09-21：后端逻辑第二轮复核

再次按现有模块做基线、失败复现、最小修复和隔离产品验收。本轮修复日期／跨时区／窗口／时长、邮件新旧接口证据、授权取消与关闭竞态、模型配置故障连带阻塞本地资料等 14 个失败场景。补充 17 项测试，最终 252 项 Node、78 项 Python、构建和 4 条隔离浏览器链路通过。真实四库逐表保留。

见 [第二轮验收记录](../docs/后端逻辑复核-第二轮-20260921.md)。尚需用户重启 18765 旧后台及真实邮箱／模型试点；未实现的增量队列、后台唤起和草稿等继续保留原任务，不因测试通过视为完成。

## 2026-09-21：正式桌面资料与桌宠接入

用户确认以正式台账为准。按“新资料迁移 → TS 小窗桥接 → 原生透明桌宠 → 隔离验收 → 桌面入口切换”完成本机接入。目录备注、性质和新增日期也保留在本机，18763／18765 原样保留；新桌面入口使用 18766 与 desktop-primary，不结束原预览进程。具体个人进度不公开。

已完成构建、252 项 Node、92 项 Python、原生 WebView2／桌宠和隔离 Edge 验收。桌面快捷方式已备份并切换；桌宠与新版小窗实际运行。完整设计、证据和回退见 [桌面统一升级与桌宠接入](../docs/桌面统一升级与桌宠接入-20260921.md)。通用安装包、更多角色动作、长期多屏测试及真实邮箱／模型授权不在此次接入范围。
